version: 1
frontend:
  phases:
    build:
      commands:
        - yarn install
        - gatsby build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
  # customHeaders:
  #   - pattern: '**/*'
  #     headers:
  #     - key: 'Cache-Control'
  #       value: 'no-store'
