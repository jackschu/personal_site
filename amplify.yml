version: 1
frontend:
  phases:
    build:
      commands:
        - yarn install
        - gatsby build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
